from pyrogram import Client, filters

# Create a new Pyrogram client
app = Client("my_userbot")

# Define an "alive" command handler
@app.on_message(filters.command("alive"))
def alive_command_handler(client, message):
    # Reply with a message indicating that the userbot is alive
    message.reply_text("I'm alive!")

# Start the client
app.run()
